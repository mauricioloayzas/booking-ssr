{
  "name": "clichin-booking-ssr",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "preview": "node .output/server/index.mjs",
    "postinstall": "nuxt prepare",
    "deploy:dev": "nuxt build --dotenv .env && AWS_PROFILE=mauloasan aws s3 sync .output/public/ s3://clichin-booking-assets-dev/ --delete --cache-control \"max-age=31536000,public\" && AWS_PROFILE=mauloasan npx sls deploy --stage dev",
    "deploy:prod": "nuxt build && AWS_PROFILE=mauloasan aws s3 sync .output/public/ s3://clichin-booking-assets-prod/ --delete --cache-control \"max-age=31536000,public\" && AWS_PROFILE=mauloasan npx sls deploy --stage prod"
  },
  "dependencies": {
    "nuxt": "^3.21.2"
  },
  "devDependencies": {
    "serverless": "^3.38.0",
    "serverless-offline": "^13.6.0",
    "typescript": "^5.0.0"
  }
}
